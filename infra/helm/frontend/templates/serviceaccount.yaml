apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
# Frontend does not call the Kubernetes API.
# Disabling token mounting removes a credential a compromised pod could abuse.
automountServiceAccountToken: false
