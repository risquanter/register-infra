apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "irmin.labels" . | nindent 4 }}
# Irmin does not call the Kubernetes API.
# Disabling token mounting removes a credential a compromised pod could abuse.
automountServiceAccountToken: false
